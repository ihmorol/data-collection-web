# ⚡ Lite Team — Quick Prompts

> 9 prompts — one per skill. Copy, fill in `[PLACEHOLDERS]`, go.

---

## 1. Plan → `@concise-planning`
```
Use @concise-planning. I want to build [APP].
Target: [WHO]. Features: [LIST]. Constraints: [TIME/BUDGET].
Create scope (in/out), tech stack, and action items.
```

---

## 2. Architecture → `@senior-architect`
```
Use @senior-architect. Create a system architecture for [APP].
Stack: [TECH]. Include: component diagram, data flow,
database design, and deployment topology.
```

---

## 3. Design → `@ui-ux-pro-max`
```
Use @ui-ux-pro-max. Create a design system for [APP TYPE].
Style: [PREFERENCE]. Include: colors, typography, spacing,
component tokens. Output as CSS variables.
```

---

## 4. Scaffold → `@app-builder`
```
Use @app-builder. Scaffold a [FRAMEWORK] project.
Type: [APP TYPE]. Set up: file structure, routing,
linting, formatting, path aliases.
```

---

## 5. Build → `@senior-fullstack`
```
Use @senior-fullstack. Build [FEATURE/PAGE].
Frontend: [COMPONENTS NEEDED]. Backend: [API ENDPOINTS].
Data: [ENTITIES]. Follow production patterns.
```

---

## 6. Database → `@database-design`
```
Use @database-design. Design the schema for [APP].
Entities: [LIST]. Relationships: [DESCRIBE].
ORM: Prisma. Include: migrations, indexes, seed data.
```

---

## 7. Code Review → `@clean-code`
```
Use @clean-code. Review the codebase.
Check: SRP, DRY, dead code, missing types, broken imports.
Refactor anything that doesn't meet standards.
```

---

## 8. Test → `@webapp-testing`
```
Use @webapp-testing. Write e2e tests for [CRITICAL FLOW].
Steps: [USER JOURNEY]. Assert: [WHAT TO CHECK].
Include: login setup, test data, cleanup.
```

---

## 9. Deploy → `@vercel-deployment`
```
Use @vercel-deployment. Deploy to Vercel.
Env vars: [LIST]. Configure: build command, region,
custom domain, preview deployments.
```
